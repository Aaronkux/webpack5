import React from 'react';
import { useHistory } from 'react-router-dom';

export default function DashBoard() {
  const history = useHistory();
  return <div style={{ textAlign: 'center' }}>Designing</div>;
}
